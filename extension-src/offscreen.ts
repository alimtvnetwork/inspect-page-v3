/**
 * Offscreen document entry. Stage 0 stub — canvas logic added in Stage 6.
 */
import { logger } from "@shared/logger";
import { LogCategory } from "@shared/enums";

logger.debug(LogCategory.Offscreen, "Offscreen document loaded");
