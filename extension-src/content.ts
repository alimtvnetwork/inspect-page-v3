/**
 * Content script entry. Stage 0 stub — handlers added in Stage 2.
 */
import { logger } from "@shared/logger";
import { LogCategory } from "@shared/enums";

logger.debug(LogCategory.Lifecycle, "Content script loaded");
